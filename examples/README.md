# examples

Runnable demos that exercise the OpenAdSource stack end-to-end.

- [`test-player/`](test-player/) - vanilla JS page that fetches `/vast`, plays the first `<MediaFile>`, and fires the impression. The Phase 1 visual verification harness.

Future phases add more examples here (publisher integration snippets, batch import scripts, etc.).

-- 0001_init.up.sql
-- Intentionally empty placeholder. The real schema (advertisers, campaigns,
-- ads, cap_rules, etc.) lands in Phase 1 migration 0002.
-- This file exists so the golang-migrate container has a baseline to apply
-- on a fresh database without erroring on an empty migrations directory.
SELECT 1;
